# conda env create -f environment.yml
name: daops
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  - numpy>=1.16
  - xarray>=0.15
  - dask
  - netcdf4
  - rtree>=0.9
  - clisops>=0.3.0
  - elasticsearch>=7.9.1
